#include "main.h"

/**
 * mul - multiply 2 digits
 * @a: the first input to be multiplied
 * @b: the second input to be multiplied
 * Return: the product of the 2 inputs
*/

int mul(int a, int b)
{
return (a * b);
}

#include "main.h"

/**
 * set_string - g sets the value of a pointer to apointer to  a char
 * @s: pointer to array to be switched
 * @to: the pointer will be go to the s pointer
 * Return: nothing
*/

void set_string(char **s, char *to)
{
*s = to;
}
